# from torch.utils.data import DataLoader

# from data.data_set import TensorDataset

# train_loader = DataLoader(
#     TensorDataset(X_train, y_train),
#     batch_size=32,
#     shuffle=False
# )

# val_loader = DataLoader(
#     TensorDataset(X_val, y_val),
#     batch_size=32,
#     shuffle=False
# )
